[package]
name = "token-budgets-extensions"
version = "0.1.0"
edition = "2024"
description = "Adaptive estimator extensions for token-budgets"
license = "MIT"

[dependencies]
# Path: assumes token-budgets-extensions and token-budgets are sibling
# directories under ~/RustroverProjects/. Verify with:
#   ls ~/RustroverProjects/token-budgets/Cargo.toml
# If the path differs, adjust the `path = ...` value below.
token-budgets = { path = "../token-budgets" }

[dev-dependencies]